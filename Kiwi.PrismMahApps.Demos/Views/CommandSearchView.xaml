<UserControl
    x:Class="Kiwi.PrismMahApps.Demos.Views.CommandSearchView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mvvm="http://prismlibrary.com/"
    xmlns:prismMahApps="clr-namespace:Kiwi.PrismMahApps;assembly=Kiwi.PrismMahApps"
    mvvm:ViewModelLocator.AutoWireViewModel="True"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance viewModels:CommandSearchViewModel}">
    <Grid>
        <ListBox
            ItemsSource="{x:Static prismMahApps:SearchableCommand.Commands}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Button
                        Margin="1"
                        Content="{Binding Path=Name}" 
                        Command="{Binding Path=.}"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>